# Frontend environment variables

# Base URL for API requests. Leave unset to use same-origin '/api' (recommended with reverse proxy).
# VITE_API_BASE_URL=https://api.yourdomain.com/api

# Socket.IO server URL. Leave unset to use same-origin (recommended with reverse proxy).
# VITE_SOCKET_URL=https://api.yourdomain.com

# Example: Local development against backend at http://localhost:5000
# VITE_API_BASE_URL=https://photo-marathon-8rkp.onrender.com/api
# VITE_SOCKET_URL=https://photo-marathon-8rkp.onrender.com
